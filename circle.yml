machine:
  xcode:
    version: 8.3
dependencies:
  pre:
    - brew tap schwa/punic
    - brew install punic
    - punic build
  cache_directories:
    - "~/Library/Application Support/io.schwa.punic/"